server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: ms-pedidos

  r2dbc:
    url: r2dbc:postgresql://${DB_HOST:db.mpfpbdyxlaawonjhhikw.supabase.co}:${DB_PORT:5432}/${DB_NAME:postgres}?sslMode=${DB_SSL_MODE:require}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:-kbE.8h6Un-EmmN}